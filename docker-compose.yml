services:
  web:
    build: .
    container_name: myweblogc
    restart: unless-stopped
    ports:
      - "${APP_PORT:-5000}:5000"
    environment:
      - MONGO_URI=${MONGO_URI}
      - SECRET_KEY=${SECRET_KEY}
      - FLASK_ENV=${FLASK_ENV:-production}
